"use client";
import Link from "next/link";
import { useState } from "react";
export default function RegisterPage(){const [form,setForm]=useState({name:"",email:"",password:"",school:"",classLevel:"6",rollNumber:"",academicYear:"2026-27"});const [message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMessage("Creating account...");const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();setMessage(d.message||d.error)}return <main className="auth-shell"><div className="auth-card"><Link className="brand" href="/"><span className="brand-mark">∑</span><span>mathlet</span></Link><div className="eyebrow">CREATE ACCOUNT</div><h1>Start learning.</h1><form onSubmit={submit}>{Object.entries(form).map(([key,value])=><input key={key} required type={key==="password"?"password":key==="email"?"email":"text"} placeholder={key==="classLevel"?"Class level":key==="academicYear"?"Academic year":key.replace(/([A-Z])/g," $1")} value={value} onChange={e=>setForm({...form,[key]:e.target.value})}/>)}<button className="complete-button">Create account</button></form><p className="auth-message">{message}</p><p className="auth-link">Already registered? <Link href="/login">Sign in</Link></p></div></main>}